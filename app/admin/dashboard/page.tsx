import {metrics} from '@/lib/demo-data';import {formatINR} from '@/lib/money';import {AdminLayout,ChartCard,DashboardCard,DateFilter} from '@/components/ui';
export default function Dashboard(){const m=metrics();return <AdminLayout><h1>Financial Dashboard</h1><DateFilter/><div className="grid cards">{[['Total Collection',m.total],['Verified Collection',m.verified],['Pending Collection',m.pending],['Cash Collection',m.cash],['Online Collection',m.online],['Total Expenses',m.expenses],['Total Withdrawals',m.withdrawals],['Available Balance',m.balance]].map(([t,v])=><DashboardCard key={t as string} title={t as string} value={formatINR(v as number)}/>)}</div><div className="grid cards"><DashboardCard title="Today’s Collection" value={formatINR(120200)}/><DashboardCard title="This Week" value={formatINR(625500)}/><DashboardCard title="This Month" value={formatINR(m.verified)}/><DashboardCard title="Total Donors" value={String(m.donors)}/><DashboardCard title="Average Donation" value={formatINR(m.avg)}/></div><div className="grid cards">{['Daily collection','Monthly collection','Online vs Cash','Expenses by category','Collection trend'].map(x=><ChartCard key={x} title={x}/>)}</div></AdminLayout>}
