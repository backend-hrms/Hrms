import {expenses} from '@/lib/demo-data';import {formatINR} from '@/lib/money';import {AdminLayout,DashboardCard,ChartCard} from '@/components/ui';import {ExpenseForm} from '@/components/forms';
export default function Expenses(){const total=expenses.reduce((s,e)=>s+e.amount,0);return <AdminLayout><h1>Expense Management</h1><div className="grid cards"><DashboardCard title="Total expenses" value={formatINR(total)}/><ChartCard title="Category-wise expenses"/></div><ExpenseForm/><h2>Recent expenses</h2><div className="table-wrap"><table className="table"><tbody>{expenses.map(e=><tr key={e.id}><td>{e.title}</td><td>{e.category}</td><td>{formatINR(e.amount)}</td><td>{e.paidBy}</td></tr>)}</tbody></table></div></AdminLayout>}
