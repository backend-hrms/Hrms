import {mandal} from '@/lib/demo-data';import {DonationForm} from '@/components/forms';
export default function Donate(){return <main><section className="hero"><div className="container"><h1>{mandal.logo} Support Our Ganpati Festival</h1><p>{mandal.name} • Festival {mandal.year}</p><p>{mandal.address} • {mandal.contact}</p></div></section><div className="container grid" style={{gridTemplateColumns:'repeat(auto-fit,minmax(300px,1fr))'}}><div className="card"><h2>Secure digital chanda</h2><p>Your payment is marked successful only after server-side provider verification or a signed webhook. Receipts are generated only for VERIFIED donations.</p></div><DonationForm/></div></main>}
