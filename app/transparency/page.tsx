import {metrics} from '@/lib/demo-data';import {formatINR} from '@/lib/money';import {DashboardCard,ChartCard} from '@/components/ui';
export default function Transparency(){const m=metrics();return <main className="container"><h1>Public Transparency</h1><p>No donor names, phone numbers, addresses or private transaction IDs are exposed.</p><div className="grid cards"><DashboardCard title="Total Donations" value={String(m.donors)}/><DashboardCard title="Total Collection" value={formatINR(m.verified)}/><DashboardCard title="Total Expenses" value={formatINR(m.expenses)}/><DashboardCard title="Total Withdrawals" value={formatINR(m.withdrawals)}/><DashboardCard title="Current Balance" value={formatINR(m.balance)}/></div><div className="grid cards"><ChartCard title="Monthly collection"/><ChartCard title="Expense breakdown"/></div><p>Last updated: {new Date().toLocaleString('en-IN')}</p></main>}
