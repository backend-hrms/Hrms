import crypto from 'node:crypto';
export type PaymentOrder={id:string;amount:number;currency:'INR';provider:'development'|'razorpay';status:'created'};
export interface PaymentService{createOrder(amount:number,metadata:Record<string,string>):Promise<PaymentOrder>;verifyPayment(payload:unknown,signature:string):Promise<boolean>;handleWebhook(body:string,signature:string):Promise<{paymentId:string;transactionId:string;verified:boolean}>;refundPayment(paymentId:string,amount:number):Promise<{refundId:string}>}
export class DevelopmentPaymentService implements PaymentService{async createOrder(amount:number){return{id:`dev_order_${crypto.randomUUID()}`,amount,currency:'INR' as const,provider:'development' as const,status:'created' as const}}async verifyPayment(){return process.env.NODE_ENV!=='production'}async handleWebhook(body:string,signature:string){const expected=crypto.createHmac('sha256',process.env.PAYMENT_WEBHOOK_SECRET||'dev-webhook-secret').update(body).digest('hex');return{paymentId:'dev_payment',transactionId:'dev_txn',verified:process.env.NODE_ENV!=='production'||signature===expected}}async refundPayment(){return{refundId:`dev_refund_${crypto.randomUUID()}`}}}
export function getPaymentService():PaymentService{return new DevelopmentPaymentService()}
